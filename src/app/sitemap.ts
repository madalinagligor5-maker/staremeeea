import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const b = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ritualuldeazi.ro"; return ["", "/despre", "/instrumente", "/resurse", "/blog", "/preturi", "/contact", "/confidentialitate", "/termeni"].map(path => ({ url: `${b}${path}`, lastModified: new Date(), changeFrequency: path === "" ? "weekly" : "monthly", priority: path === "" ? 1 : .7 })); }
